# 컨텍스트 노트

## 결정 사항

### 단일 파일 구조
- `index.html`에 CSS/JS 전부 인라인
- 이유. 더블클릭 실행 가능해야 함 (file:// 프로토콜). ES 모듈은 CORS 막힘
- 트레이드오프. 파일 비대해지지만 MVP 단계라 OK. 나중에 번들러 도입 시 분리

### 캔버스 해상도
- 내부 1280x720 고정, CSS로 화면 맞춤
- `image-rendering: pixelated`로 픽셀 보존
- 스프라이트는 컬러 도형 플레이스홀더 (사각형/원/조합)

### 낚시 상태머신
- IDLE → CASTING → HOOKED → REELING → IDLE
- CASTING. 갈고리 자동 하강, ←/→ 좌우 미세조정, 줄 길이 한도 도달 시 자동 REELING 전환
- HOOKED. 충돌 시 즉시 진입, 텐션 게이지 노출
- REELING. Space 연타로 위로 끌어올림, 텐션 한도 초과 시 줄 끊김 → IDLE 복귀 (물고기 손실)

### 텐션 공식 (MVP)
- Space 누를 때마다 reel +8px, tension += fish.struggle
- 매 프레임 tension -= 0.5 (자연 감소)
- tension >= 100 → 줄 끊김
- 단순화 위해 발버둥 패턴은 일단 일정 감쇠로만. 추후 사인파 변동 추가 가능

### 인벤토리
- 슬롯 5개 시작, 업그레이드로 +
- 풀이면 캐스팅 막힘 (HUD 경고)
- 처리는 상점 탭에서

### 수족관 패시브
- 슬롯에 배치된 물고기 = price * 0.001 골드/초
- 희귀어일수록 장기수익 유리. price 자체가 커서 자동 충족
- 마지막 접속 시각 저장 → 재방문 시 누락분 누적 (오프라인 수익)

### 저장
- localStorage 키 `fishingGame.v1`
- 매 5초 자동저장 + 탭 전환/판매/업그레이드 시 즉시 저장
- 초기화 = 키 삭제 + reload

## 추후 확장 포인트
- `FISH_DB` 배열에 객체 추가만 하면 자동 스폰 테이블 반영
- 스프라이트 함수 `drawFishSprite(ctx, fish)` 한 곳에서 분기. 실제 이미지 교체 시 여기만 수정
- 업그레이드는 `UPGRADES` 객체에 항목 추가하면 상점 자동 렌더
- 깊이 티어 경계는 `DEPTH_TIERS` 상수
